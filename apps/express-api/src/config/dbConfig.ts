// import { CONFIG } from './app.config';
import { ConnectOptions } from 'mongoose';
export const connectOptions: ConnectOptions = {
  // dbName: `app-data-${CONFIG.MONGO_DB_NAME}`,
  // user: CONFIG.MONGO_USERNAME,
  // pass: CONFIG.MONGO_PASSWORD,
};
